package dianapc;

/**
 * We need a program to manage contacts in an electronic diary.
 *
 * a. The following data must be allowed to be entered in the agenda: ID, first
 * name, last name, date of birth, telephone number and address (each in its
 * corresponding textField).
 *
 * b. Data of 10 people need to be stored. To do this, the use of 1 vector is
 * proposed to store the values of each of the fields (taking into account that
 * there may be different types of data), where each index will indicate the
 * data of a certain person in all the vectors.
 *
 * c. The program must allow values to be loaded in a certain position of the
 * vector (by clicking on the load button) and traverse the vector finding the
 * loaded data.
 *
 * d. By clicking on the right and left arrows you must increase (or decrease)
 * the index and with it the data corresponding to the user (this same logic is
 * used to load the data).
 *
 *
 *
 * Se necesita un programa para gestionar los contactos de una agenda
 * electrónica.
 *
 * a. En la agenda se debe permitir ingresar los siguientes datos: DNI, nombre,
 * apellido, fecha de nacimiento, teléfono y dirección (cada uno en su
 * correspondiente textField).
 *
 * b. Se necesitan Almacenar los datos de 10 personas. Para ello se propone la
 * utilización de 1 vector para almacenar los valores de cada uno de los campos
 * (teniendo en cuenta que pueden existir distintos tipos de datos), en donde
 * cada índice indicará los datos de una determinada persona en todos los
 * vectores.
 *
 * c. El programa debe permitir cargar valores en una determinada posición del
 * vector (al hacer click en el borón cargar) y recorrer el vector encontrando
 * los datos cargados.
 *
 * d. Al hacer click en las flechas de derecha e izquierda se debe incrementar
 * (o decrementar) el índice y con ello los datos correspondientes al usuario
 * (esta misma lógica se utiliza para cargar los datos).
 *
 */
public class ElectronicPhonebook {

    public static void main(String[] args) {
        
        

    }
}
